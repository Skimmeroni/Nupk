# Nupk
Installs packages. Requires:

- Basic utilities: `basename`, `cd`, `cp`, `cut`, `grep`, `id`, `mv`, `printf`, `read`, `rm`, `rmdir`, `sed`, `tr`, `unlink`
- `curl` (only used for version update)
- `jq` (only used for version update)
- a POSIX-compliant shell
